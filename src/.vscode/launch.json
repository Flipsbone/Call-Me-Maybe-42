{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Déboguer le générateur LLM",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/src/__main__.py", 
            "console": "integratedTerminal",
            "justMyCode": true,
            "cwd": "${workspaceFolder}"
        }
    ]
}